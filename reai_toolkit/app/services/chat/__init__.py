"""Agent Chat feature — streaming conversational AI agent.

Ports the Dashboard's Agent Chat subsystem (event schema, reducer state machine
and SSE streaming client) onto the vendored ``revengai.ConversationsApi``.
"""
